# Strat-pipeline change ledger

| ID | Source | Purpose | Upstream candidate | Status |
| --- | --- | --- | --- | --- |
| M3-001 | `.gitlab-ci.yml` | Resolve `strat-creator` and `assess-strat` from a runtime GitHub URL, while retaining public GitHub repository defaults and the existing public Jira default. | Yes | Applied to emulator mirror |
| M3-002 | `ci-scripts/clone-data-repo.sh` | Construct authenticated results-repository URLs from `GITLAB_GIT_URL`. | Yes | Applied to emulator mirror |
| M3-003 | `ci-scripts/push-results.py` | Construct authenticated push URLs from `GITLAB_GIT_URL`. | Yes | Applied to emulator mirror |
| M3-004 | Fixture CI variables | Supply in-cluster GitHub, GitLab, and Jira service URLs to jobs. | Environment-specific | Applied |
| M3-005 | `strat-creator` Jira, Git, and curl clients | Honor opt-in `NO_SSL_VERIFY=1` for connections to self-signed emulator certificates, while retaining certificate verification by default. | Yes | Applied to GitHub emulator mirror |
| M3-006 | `strat-pipeline` CI and helper scripts | Propagate opt-in `NO_SSL_VERIFY=1` to Git transport used by pipeline jobs; default remains secure verification. | Yes | Applied to GitLab emulator mirror |
| M3-007 | Fixture CI variable `NO_SSL_VERIFY` | Set `NO_SSL_VERIFY=1` only in the emulator project so client jobs can connect to the emulator TLS endpoints; this is not an emulator deployment setting. | Environment-specific | Applied |
| M3-008 | `strat-pipeline` `.pipeline-setup` shell environment | Derive `CLAUDE_REPO` and `CLAUDE_PLUGINS` at job runtime from `GITHUB_GIT_URL`; this avoids relying on nested CI-variable expansion while retaining public GitHub defaults. | Yes | Applied to GitLab emulator mirror |
| M3-009 | `.gitlab-ci.yml` variable precedence | Do not define `GITHUB_GIT_URL` in YAML: the emulator currently lets YAML variables mask project variables. The shell fallback still makes an ordinary upstream checkout use public GitHub without extra settings. | Yes | Applied to GitLab emulator mirror |
| M3-010 | `.gitlab-ci.yml` and `.pipeline-setup` shell environment | Apply the same runtime-resolution rule to Jira and Vertex variables: remove nested YAML references that the emulator masks, then use existing `JIRA_API_TOKEN` and `GCP_PROJECT_ID` values with public Jira fallback. | Yes | Applied to GitLab emulator mirror |
| M3-011 | `.gitlab-ci.yml` and client TLS environment | Do not define `NO_SSL_VERIFY` in YAML: the emulator’s YAML precedence previously masked the project’s `NO_SSL_VERIFY=1`. Client code still defaults to verification when the variable is absent. | Yes | Applied to GitLab emulator mirror |
| M3-012 | Upstream `strat-creator` PR #61 (`CLAUDE.md` and closed bug report) | Scope repository-test instructions to implementation changes and explicitly exempt strategy artifacts, Jira updates, and other workflow output. This prevents artifact-only strategy jobs from launching pytest. | Yes | Merged upstream as `5761024`; current combined adapted mirror `1b7532f` |
| M3-013 | `strat-dashboard/.gitlab-ci.yml` | Derive the creator checkout from `GITHUB_GIT_URL`, derive the data checkout from `GITLAB_GIT_URL` and `DATA_REPO_TOKEN`, and honor opt-in `NO_SSL_VERIFY=1`; public GitHub/GitLab defaults remain unchanged. | Yes | Applied as tracked patch `0003`; current adapted mirror `f1ace7a` |
| M3-014 | Dashboard project CI variables | Store emulator Git endpoints and the masked `DATA_REPO_TOKEN` on `strat-dashboard`; reuse the existing GitLab PAT rather than creating a second credential. | Environment-specific | Applied by M2 bootstrap |
| M3-015 | Mirror publisher remote selection | Publish `strat-dashboard` to its own nested GitLab project instead of reusing the `strat-pipeline` remote. | Breadboard fixture | Fixed and validated |
| M3-016 | `strat-creator/scripts/jira_utils.py` attachment downloader | Rewrite emulator-generated loopback attachment URLs to the configured `JIRA_SERVER` origin so runner pods can download review attachments; leave normal upstream URLs unchanged. | Environment-specific emulator compatibility | Applied as tracked patch `0004`; current adapted mirror `1b7532f` |
| M3-017 | Adapted `strat-creator/config/emulator-smoke.yaml` | Provide one controlled RFE key for repeatable `batch-config` validation without changing the private source checkout or requiring production Jira data. | Breadboard fixture | Applied as tracked patch `0005` |
| M3-018 | `strat-pipeline/ci-scripts/pipeline-post.sh` no-work path | Treat a valid batch that creates no new strategies (for example, all RFEs already processed) as successful instead of failing report generation with “no strategy artifacts found.” Normal artifact publication remains unchanged. | Yes | Applied as tracked patch `0006`; current adapted mirror `975ccf3` |
| M3-019 | `strat-pipeline` Claude launcher and CI artifacts | Enable opt-in `OTEL_LOG_RAW_API_BODIES=file:...` capture through `CAPTURE_API_BODIES=1`; preserve body files as restricted GitLab job artifacts alongside `claude-otel.jsonl`, without printing or publishing them to `strat-pipeline-data`. | Yes for the generic launcher/artifact behavior; fixture variable is environment-specific | Applied as tracked patch `0007`; bootstrap enables the fixture project variable; runtime validation pending |
| M5-001 | `reprocess-strat` against `RHAISTRAT-2` | Verify attachment pull, refine, review, unlock, and result publication using the Jira loopback-URL adaptation. | Validation | Pipeline `1873`, job `3723` succeeded |
| M5-002 | `batch-config` with `config/emulator-smoke.yaml` | Verify the config-file path and the already-processed RFE skip path. The first run exposed M3-018; the corrected rerun completed successfully. | Validation / Yes for M3-018 | Initial job `3727` failed at post-processing; corrected job `3734` succeeded |
| M5-003 | Scheduled `batch-jql` | Verify the schedule source and scheduled job materialization with `BATCH_SIZE=0`, preventing unintended production-key processing. | Validation | Pipeline `1878`, job `3730` succeeded with `source=schedule` |
| M5-004 | GitLab emulator stale-threshold banner | A long Claude job can display a stale/pending warning while its runner pod is active; both `reprocess-strat` and `batch-jql` subsequently completed successfully without requeue. | Emulator behavior | Observed; no requeue required |

The `strat-pipeline` private source checkout remains at its recorded baseline.
The local `strat-creator` checkout is rebased to upstream `main`; the adapted
mirrors are rebuilt in temporary clones by applying only the tracked
environment patches.
